# Audio Fixture Manifest

These WAV files are generated by Csound and are not engine dependencies.

Generate or refresh them with:

```sh
bash tools/csound/generate-fixtures.sh
```

Expected generated files:
- `kick.wav`
- `snare.wav`
- `hat.wav`
- `sub.wav`

The engine should treat these as ordinary sample fixtures once sample loading is implemented.
